# Stage 9A candidate selection — cycle 6 (next-priority evaluation)

Status: pre-result. Per [next-priority-evaluation.md](../next-priority-evaluation.md),
first use of that template. No comparative backtest run or inspected.

## Inventory

| Candidate | Status | Blocker |
|---|---|---|
| Fed put (purchase/buyback-conditioned long-end yield reversal) | operationalized ([memo](../brainstorm/2026-08-19-fed-put-long-end-reversal.md)) | data partial, design unbuilt (below) |
| Broader macro reaction-function library (6 other variables) | idea-stage only | no operationalization record per variable — [hypothesis-engineering.md](../hypothesis-engineering.md) gate not met, not scoreable this round |
| Cross-sectional equity momentum extension | engine-feasible (`0.61.0`) | Tier 4 unpurchased PIT universe data |

## Scores (model-acceptance-standard, 0/1/2)

**Cross-sectional**: Data readiness `0` by construction (survivorship-biased universe, no PIT data) — disqualified per the standard's own explicit rule. Engine feasibility doesn't change this.

**Fed put**:

| Dimension | Score | Note |
|---|---:|---|
| Rationale | 2 | Gagnon 2011, Krishnamurthy-Vissing-Jorgensen 2011, D'Amico-King 2013 |
| Product relevance | 2 | first true macro/policy-conditioned candidate |
| Distinct information | 2 | untested information class this project |
| Data readiness | 1 | `TREAST`/`TREAS10Y` (Fed SOMA Treasury holdings, incl. >10y bucket) confirmed live on FRED, ingestible today via `macro_pit`. Treasury buyback ops **not** on FRED — separate free, keyless API (`fiscaldata.treasury.gov`), not yet wired |
| Implementability | 1 | needs a small-*n* Thesis Track design (~5-10 regime episodes); this project's only machinery to date is block-bootstrap, wrong tool here |
| Falsifiability | 2 | explicit H0/H1 in memo |
| Research restraint | 2 | single hypothesis, excludes Fed-intent narrative by design |
| Diversification | 2 | macro/policy, orthogonal to every tested candidate |
| **Total** | **14/16** | |

## Decision

Fed put is the only scoreable, eligible candidate this cycle — not disqualified like cross-sectional, not yet-formalized like the other 6 macro variables. **Not preregistered yet**: Data readiness and Implementability are `1`, not `2`. Next engineering steps, in cost order, before preregistration:

1. Ingest `TREAST`/`TREAS10Y` via `macro_pit` (cheap, engine exists).
2. New ingestion module for `fiscaldata.treasury.gov`'s buyback dataset (free, keyless REST API, not yet built).
3. Design the Thesis Track small-*n* statistical method (the real work — no existing precedent in this codebase).

The 6-variable macro library remains parked at idea-stage; promoting any one needs its own operationalization record first, same gate Fed put already passed.

## Update (2026-08-20): reframed, buybacks dropped

Steps 1-3 above are done (`0.63.0`). Two corrections after, both
user-directed, neither changes the 14/16 score:

- Claim reframed from "Fed support → yield reversal" (well-known, not a
  real edge) to "yield stress → precedes Fed QE" (forward-looking
  precursor). Same episode set (QE1/2/3, COVID QE), reversed direction.
- Treasury buybacks dropped entirely — different institution/mandate
  than the Fed (fiscal vs. monetary, administration-dependent vs.
  independent-agency). `app.treasury_buybacks` remains built and
  live-verified; simply unused by this candidate now. Data readiness's
  `1` score stood on `TREAST`/`TREAS10Y` availability regardless, so the
  score is unaffected — see the reframed
  [memo](../brainstorm/2026-08-19-fed-put-long-end-reversal.md).

## Update (2026-08-20): preregistered, executed, closed `not_evaluable`

[Locked protocol](../research-protocols/fed-put-yield-stress-precursor-v1.md),
[result](../research-results/fed-put-yield-stress-precursor-v1.md).
`p=0.989`, `not_evaluable` per the pre-committed reading rule. Disclosed,
non-gating diagnostic: `4`/`4` real QE episodes were decisively
*opposite*-signed, not merely null — every real launch was preceded by
both `2Y` and `10Y` falling sharply together (flight-to-safety), not
"`10Y` high, `2Y` contained." The motivating narrative (long end elevated
on term-premium/fiscal concerns) has no precedent among the 4 episodes
tested. This closes Fed put's active-research status; a follow-up would
need a new, independently-justified mechanism, not a retry of this one.
