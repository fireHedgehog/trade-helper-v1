# Agent entry point

Read [docs/README.md](docs/README.md) first, every session. It is the authoritative checkpoint: version, state, next task, non-negotiable state, environment/data-portability rules, and document map.

This file is a routing pointer, not a checkpoint. `AGENTS.md` and `CLAUDE.md` at the repository root are kept identical so any agent tool finds the same instruction regardless of which filename it reads. Edit `docs/README.md` for checkpoint changes, never this file.
